meta {
  name: delete pricing zone
  type: http
  seq: 6
}

delete {
  url: {{serviceCatalogUrl}}/pricing-zones/:zoneId
  body: none
  auth: inherit
}

params:path {
  zoneId: {{zoneId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
