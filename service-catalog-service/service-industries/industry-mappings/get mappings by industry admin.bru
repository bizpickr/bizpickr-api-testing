meta {
  name: get mappings by industry (admin)
  type: http
  seq: 2
}

get {
  url: {{serviceCatalogUrl}}/industries/:id/mappings/admin
  body: none
  auth: inherit
}

params:path {
  id: {{industryId}}
}

headers {
  x-auth-context: admin
}

settings {
  encodeUrl: true
  timeout: 0
}
