meta {
  name: delete mapping
  type: http
  seq: 8
}

delete {
  url: {{serviceCatalogUrl}}/industries/:id/mappings/:mappingId
  body: none
  auth: inherit
}

params:path {
  id: {{industryId}}
  mappingId: {{mappingId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
