meta {
  name: create mapping
  type: http
  seq: 2
}

post {
  url: {{serviceCatalogUrl}}/industries/:id/mappings
  body: json
  auth: inherit
}

params:path {
  id: {{industryId}}
}

body:json {
  {
    "serviceId": "{{serviceId}}",
    "icon": "🌐",
    "title": "Medical Website",
    "description": "Professional website for clinic or hospital",
    "sortOrder": 0
  }
}

vars:pre-request {
  serviceId: 0513df4f-fbf6-4bb7-a2ab-a277acf1b860
}

script:post-response {
  if (res.status >= 200 && res.status < 300 && res.body?.data?.id) {
    bru.setEnvVar("mappingId", res.body.data.id);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
