meta {
  name: archive mapping
  type: http
  seq: 7
}

patch {
  url: {{serviceCatalogUrl}}/industries/:id/mappings/:mappingId/archive
  body: none
  auth: inherit
}

params:path {
  id: {{industryId}}
  mappingId: {{mappingId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
